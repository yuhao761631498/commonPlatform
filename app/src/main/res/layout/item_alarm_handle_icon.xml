<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="10dp"
    android:layout_marginRight="10dp">

    <ImageView
        android:id="@+id/iv_alarm_detail_icon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_name"
        android:minHeight="54dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_pic_placehold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_videoPlayIcon"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:contentDescription="@string/app_name"
        android:gravity="center"
        android:minWidth="19dp"
        android:minHeight="19dp"
        android:src="@drawable/ic_alarm_play"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/iv_alarm_detail_icon"
        app:layout_constraintLeft_toLeftOf="@+id/iv_alarm_detail_icon"
        app:layout_constraintRight_toRightOf="@+id/iv_alarm_detail_icon"
        app:layout_constraintTop_toTopOf="@+id/iv_alarm_detail_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>